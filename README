Install django-messages:
  sudo pip install django-messages

Install redis:
  sudo pip install redis
Install celery:
  sudo pip install django-celery
  
Sending periodic email notifications:
Run each command on a different window
  redis-server
  celery -A ecs160 worker -l info
  celery -A ecs160 beat -l info